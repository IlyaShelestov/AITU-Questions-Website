<%- include('partials/header', { title: 'Войти', customCss: 'login' }) %>

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <i data-lucide="shield-check" class="auth-icon"></i>
            <h1>Войти в AITU Connect</h1>
            <p>Добро пожаловать обратно! Войдите в свой аккаунт.</p>
        </div>
        
        <% if (error) { %>
            <div class="alert alert-error">
                <i data-lucide="alert-circle"></i>
                <span><%= error %></span>
            </div>
        <% } %>
        
        <form action="/auth/login" method="POST" class="auth-form">
            <div class="form-group">
                <label for="email">
                    <i data-lucide="mail"></i>
                    Email
                </label>
                <input type="email" id="email" name="email" class="form-control" placeholder="Введите ваш email" required>
            </div>
            
            <div class="form-group">
                <label for="password">
                    <i data-lucide="lock"></i>
                    Пароль
                </label>
                <input type="password" id="password" name="password" class="form-control" placeholder="Введите ваш пароль" required>
            </div>
            
            <button type="submit" class="btn btn-primary btn-full">
                <i data-lucide="log-in"></i>
                Войти
            </button>
        </form>
        
        <div class="auth-footer">
            <p>Нужна помощь? Обратитесь к администратору.</p>
        </div>
    </div>
</div>

<%- include('partials/footer') %>